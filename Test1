import streamlit as st

# ตั้งค่าหน้ากระดาษ
st.set_page_config(layout="wide")

# ใช้ CSS เพื่อปรับแต่งหน้าตาให้เหมือนสมุดบันทึก
st.markdown("""
    <style>
    .main {
        background-color: #fdfaf0; /* สีออกครีมเหมือนกระดาษ */
        font-family: 'Sarabun', sans-serif;
    }
    .sidebar-content {
        border-right: 2px dashed #333;
        padding-right: 20px;
    }
    .title-box {
        border: 2px solid #003366;
        padding: 20px;
        text-align: center;
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 10px;
    }
    .nav-tabs {
        display: flex;
        border: 1px solid #003366;
        width: fit-content;
    }
    .nav-item {
        padding: 5px 15px;
        border-right: 1px solid #003366;
        font-size: 14px;
    }
    .nav-item-active {
        background-color: #003366;
        color: white;
    }
    .list-box {
        border: 2px solid #003366;
        border-radius: 15px;
        padding: 15px;
        min-height: 400px;
        margin-top: 20px;
    }
    </style>
    """, unsafe_allow_html=True)

# --- ส่วนหัว (Header) ---
st.write(f"### BioSmart Classroom V.1")
st.markdown("---")

# --- การจัด Layout แบ่งซ้ายขวา ---
col_left, col_divider, col_right = st.columns([1, 0.1, 3])

with col_left:
    st.write("**Menu:**")
    st.write("① สำหรับนักเรียน")
    st.write("② สำหรับครู")
    
    # ส่วนของกล่องรายการ (ป้ายประกาศ)
    st.markdown("""
        <div class="list-box">
            <p style="text-align:center; text-decoration: underline;">ประกาศ</p>
            <p>✓ </p>
            <p>✓ </p>
            <p>✓ </p>
            <p>✓ </p>
            <p>✓ </p>
        </div>
    """, unsafe_allow_html=True)

with col_divider:
    # เส้นประคั่นกลาง (จำลองจากภาพ)
    for _ in range(15):
        st.write("|")

with col_right:
    # ส่วน Title
    st.markdown('<div class="title-box">Title</div>', unsafe_allow_html=True)
    
    # ส่วนของแถบเมนูย่อย (Tabs)
    st.markdown("""
        <div class="nav-tabs">
            <div class="nav-item">ลงทะเบียน</div>
            <div class="nav-item">วิเคราะห์วิชา</div>
            <div class="nav-item nav-item-active">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</div>
        </div>
    """, unsafe_allow_html=True)
    
    # พื้นที่ว่างสำหรับเนื้อหา
    st.write("")
    st.info("พื้นที่สำหรับแสดงเนื้อหาหลัก...")

